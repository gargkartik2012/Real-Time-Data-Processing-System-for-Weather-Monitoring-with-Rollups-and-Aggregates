spring.application.name=Weather Monitoring
server.port=5959

# OpenWeatherMap API key
weather.api.key=${API_KEY}
weather.api.url=https://api.openweathermap.org/data/2.5/weather?q={CITY}&appid={API_KEY}

# Scheduler interval
weather.fetch.interval=300000
weather.check.interval=300000



# Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update


# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${USEREMAIL};
spring.mail.password=nvryzhlmhlkrjhfo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
